# exec = export SLURP_ARGS='-d -c FDD9FDBB -b 5A405D44 -s 00000000'

general {
    col.active_border = rgba(EBDFED39)
    col.inactive_border = rgba(998D9D30)
}

misc {
    background_color = rgba(17111BFF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(17111BFF)
        col.text = rgba(EBDFEDFF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(EBDFED), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(EBDFED), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(EBDFED), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(E2B6FFAA) rgba(E2B6FF77),pinned:1